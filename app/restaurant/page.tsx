import {createServerSupabase} from"@/lib/supabase/server";export const dynamic="force-dynamic";
export default async function RestaurantPage(){const s=createServerSupabase();const[{data:r},{data:l},{data:h}]=await Promise.all([s.from("restaurants").select("*").limit(1).single(),s.from("restaurant_locations").select("*").eq("is_active",true).limit(1).single(),s.from("restaurant_hours").select("*").order("day_of_week")]);const days=["Sunday","Monday","Tuesday","Wednesday","Thursday","Friday","Saturday"];return <div className="space-y-6"><div><p className="text-sm font-semibold text-blue-600">Restaurant profile</p><h1 className="mt-1 text-3xl font-bold">{r?.name??"Restaurant"}</h1><p className="mt-2 text-slate-500">The operational identity and location used by the live application.</p></div><div className="grid gap-6 lg:grid-cols-[.8fr_1.2fr]"><section className="rounded-2xl border border-slate-200 bg-white p-6 shadow-sm"><div className="flex items-center gap-4"><img src={r?.logo_url||"/junction-kitchen.svg"} alt="Restaurant logo" className="h-20 w-20 rounded-2xl object-cover"/><div><h2 className="font-semibold">{r?.name}</h2><p className="mt-1 text-sm text-slate-500">{r?.slug}</p><span className="mt-3 inline-flex rounded-full bg-emerald-50 px-2.5 py-1 text-xs font-semibold text-emerald-700">Active restaurant</span></div></div><dl className="mt-6 space-y-4 text-sm"><div><dt className="text-slate-400">Phone</dt><dd className="mt-1 font-medium">{r?.phone||l?.phone||"Not configured"}</dd></div><div><dt className="text-slate-400">Email</dt><dd className="mt-1 font-medium">{r?.email||l?.email||"Not configured"}</dd></div><div><dt className="text-slate-400">Timezone</dt><dd className="mt-1 font-medium">{r?.timezone||"Not configured"}</dd></div></dl></section><section className="rounded-2xl border border-slate-200 bg-white p-6 shadow-sm"><h2 className="font-semibold">Location & hours</h2><div className="mt-4 rounded-xl bg-slate-50 p-4 text-sm text-slate-600"><div className="font-medium text-slate-900">{l?.name||"Main location"}</div><div className="mt-1">{[l?.address_line1,l?.city,l?.province,l?.postal_code].filter(Boolean).join(", ")||"Address not configured"}</div></div><div className="mt-5 space-y-1">{h?.map(x=><div key={x.id} className="flex justify-between border-b border-slate-100 py-2.5 text-sm"><span>{days[x.day_of_week]}</span><span className="text-slate-500">{x.is_closed?"Closed":`${x.opens_at?.slice(0,5)} – ${x.closes_at?.slice(0,5)}`}</span></div>)}</div></section></div></div>}
