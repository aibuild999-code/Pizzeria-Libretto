import { createServerSupabase } from "@/lib/supabase/server";
export const dynamic="force-dynamic";
export default async function DeliveryPage(){const s=createServerSupabase();const {data:orders}=await s.from("orders").select("id,order_number,customer_name,delivery_address_line1,delivery_address_line2,delivery_city,delivery_province,delivery_postal_code,delivery_fee,total,status,created_at").eq("fulfillment_type","delivery").order("created_at",{ascending:false});return <div className="space-y-6"><div><p className="text-sm font-semibold text-blue-600">Operations</p><h1 className="mt-1 text-3xl font-bold">Delivery</h1><p className="mt-2 text-slate-500">Live delivery orders and configured delivery information.</p></div>{orders?.length?<div className="overflow-hidden rounded-2xl border border-slate-200 bg-white shadow-sm"><div className="divide-y divide-slate-100">{orders.map(o=><div key={o.id} className="grid gap-4 px-5 py-5 md:grid-cols-[1fr_1.4fr_auto]"><div><div className="font-semibold">#{o.order_number} · {o.customer_name}</div><div className="mt-1 text-xs capitalize text-slate-500">{o.status.replace("_"," ")}</div></div><div className="text-sm text-slate-600">{[o.delivery_address_line1,o.delivery_address_line2,o.delivery_city,o.delivery_province,o.delivery_postal_code].filter(Boolean).join(", ")||"No delivery address recorded"}</div><div className="text-left md:text-right"><div className="font-semibold">${Number(o.total).toFixed(2)}</div><div className="mt-1 text-xs text-slate-500">Fee ${Number(o.delivery_fee||0).toFixed(2)}</div></div></div>)}</div></div>:<div className="rounded-2xl border border-dashed border-slate-300 bg-white p-14 text-center"><h2 className="font-semibold">No delivery orders yet</h2><p className="mt-1 text-sm text-slate-500">Distance and delivery status will appear here when real delivery orders exist.</p></div>}<div className="rounded-2xl border border-blue-100 bg-blue-50/50 p-5 text-sm text-slate-600"><strong className="text-slate-900">Distance pricing:</strong> No distance calculation is shown until a mapping service is configured. The dashboard will not invent distances or fees.</div></div>}
